// next.config.ts

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // ✅ Eliminar eslint - ya no se usa aquí
  // eslint: {
  //   ignoreDuringBuilds: true,
  // },
  
  // Si quieres configurar ESLint, usa el archivo eslint.config.mjs
};

export default nextConfig;